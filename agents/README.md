# Runbooks de agentes

Documentar aquí la instalación, inscripción y validación de cada agente. Crear un archivo por endpoint, por ejemplo `linux-agent.md` y `windows-agent.md`.

Cada runbook debe incluir versión, configuración sanitizada, prueba de conectividad y evidencia de que el agente aparece activo en Wazuh Dashboard.
